<Window x:Class="Chem.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chem.Windows"
        mc:Ignorable="d"
        Title="LoginWindow" Height="340" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Image Source="/Resources/icon.png"
                       Width="50" Height="50" Margin="0,0,10,0"/>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="CleanStore" FontSize="25" FontWeight="Bold"/>
                    <TextBlock Text="Авторизация" FontSize="20" Opacity="0.7"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock FontSize="14" Text="Логин"/>
                <TextBox x:Name="tbLogin" Grid.Row="1" Height="28" Margin="0,3,0,10"/>

                <TextBlock FontSize="14" Text="Пароль" Grid.Row="2"/>
                <PasswordBox x:Name="pbPassword" Grid.Row="3" Height="28" VerticalAlignment="Top" Margin="0,3,0,0"/>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="Гость" Width="100" Margin="0,0,8,0" Click="BtnGuest_Click"/>
                    <Button Content="Войти" Width="100" Click="BtnLogin_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
